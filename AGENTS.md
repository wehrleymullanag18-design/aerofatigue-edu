# AeroFatigue Edu 开发约束

## 项目目标

构建面向实验教学、操作训练、安全考核和答辩展示的中文虚拟实验平台。第一版只做本地教学模拟，不连接真实仪器，不发送控制指令。

## 真实性边界

- `materials/` 是只读原始资料，不得修改、覆盖、移动或删除。
- 真实设备图片只能从 `materials/image/original/` 复制到 `public/assets/` 后使用。
- 生成辅助图必须标注“辅助设计图”，不得冒充实拍或科研成果。
- 所有曲线、传感器数值、实验结果、故障、评分均为教学模拟，页面必须显示“演示模拟数据，不代表正式实验结论。”
- 动画必须显示“动画用于解释设备工作过程，不是实拍视频。”
- 项目申请书中的预期成果、规划与技术指标不得表述为已通过独立验证。

## 开发规范

- Next.js App Router、React、严格 TypeScript、Tailwind CSS。
- 主要内容与规则集中维护在 `src/data/`；状态机与模拟引擎独立于 UI。
- 不依赖外部字体、图片 CDN、远程数据库或必须联网的 API。
- 优先适配 1920×1080 与 1366×768，并保证移动端无横向溢出。
- 本地存储读写必须容错；图片和视频缺失时使用明确占位，不得白屏。

## 测试与完成标准

- 合并前运行 `pnpm lint`、`pnpm typecheck`、`pnpm test`、`pnpm test:e2e`、`pnpm build`。
- 状态机必须阻止非法顺序；暂停、继续、急停、复位和安全联锁必须可测试。
- 模拟数据必须确定、平滑、有上限，且不产生 NaN 或 Infinity。
- Playwright 必须覆盖首页、设备热点、实验流程、安全联锁、报告与答辩切换。
- 生产构建成功、关键流程通过、浏览器控制台无明显错误后才算完成。
